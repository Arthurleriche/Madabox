import Login from './Login';

function Auth() {
  return (
    <div className="h-screen min-h-[731px] relative flex justify-center items-center">
      <Login />
    </div>
  );
}

export default Auth;
